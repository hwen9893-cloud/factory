# 23 - 预算与资源约束

> Version：v1.0　Last Update：2026-06
> Status：Active（数额为待填模板，团队据实填写后冻结）
> 依赖：`21-精益路线.md`、`32-硬件与部署方案.md`、`33-模型选型策略.md`

---

# 1. 资源现实

| 维度 | 现状 |
|------|------|
| 团队 | 3 人：剧情设计 / 美工设计 / 代码实现 |
| 已有 Capex | **本地单卡 CUDA 工作站（RTX 5090 32GB / Core Ultra 9 285K / Ubuntu，见 `32`）** |
| 平台原则 | **全本地 CUDA**：LLM/图像/视频/LoRA/音频/编排 全部本地，单卡队列错峰；云仅作可选溢出（见 `32`） |

> 本文档**不替你定额**，提供框架与红线；团队按实数填表后冻结为该版本预算线。

---

# 2. Capex（一次性支出）

| 项目 | 状态 | 建议 | 实数（待填） |
|------|------|------|-------------|
| CUDA 工作站（RTX 5090 32GB） | ✅ 已有（装机中） | 全流程本地算力核心 | 已投入 |
| **内存扩容** | ⚠️ P0 | 64GB → **128GB（256GB 更佳）**，最大短板（见 `32` §1） | ____ |
| **NVMe 扩容** | ⚠️ P1 | 加 1×2~4TB PCIe4：模型盘 + 视频 scratch 分离 | ____ |
| NAS / 异地存储 | ⚠️ 重要 | 知识库 + LoRA + 案例库异地备份（单点故障风险↑，见 §5） | ____ |
| 散热压测/改造 | 视实测 | 5090+285K 满载噪音/降频，必要时改风道 | ____ |

> 红线：**Capex 已从"买不买工作站"变为"补齐内存/存储短板"**。内存扩容优先级最高，直接决定能否写作与视频并行。

---

# 3. Opex（月度支出）

| 类别 | 项目 | 计费方式 | 月度上限（待填） |
|------|------|----------|----------------|
| **电费** | 5090+285K 满载功耗（≈700W 峰值），夜间视频批处理长时运行 | 按度（新变量，本地化后凸显） | ____ |
| 云 GPU（可选溢出） | 仅本地队列积压超阈值时按需租用 | 按量 | ____（默认 0） |
| LLM API（可选） | 写作/审校（若本地质量不足需补） | 按 token | ____（默认 0） |
| Embedding / 检索 | 本地为主（bge-m3），几乎零成本 | — | ~0 |
| 平台费 | 发布/存储/域名 + **异地备份存储** | 月费 | ____ |

> 原则变化：**云 GPU 从"主要变量"降为"可选溢出"，本地化后新增电费变量**。LLM/图/视频/TTS 全本地，边际成本主要是电费与折旧。

---

# 4. 按 Phase 的花钱节奏

| 阶段 | Capex | Opex | 说明 |
|------|-------|------|------|
| 装机期 | **内存扩 128GB+ 第 2 块 NVMe**（P0/P1） | 极小 | 补齐短板，其余按需 |
| Phase 0 | 已投入（工作站已有） | 极小（电费 + 平台费） | 内容用本地/网页工具产出 |
| Phase 1 | NAS/异地备份 | 电费 + 平台费 | 全本地自动化选定形式 |
| Phase 2 | 视需要评估第 2 张 GPU | 电费为主 + 极少云溢出 | 一致性关卡通过后本地放量 |
| Phase 3+ | 按 ROI 评估多机/GPU 集群 | 规模化后核算单集成本 | — |

---

# 5. 成本红线（信号未达标前不投）

- Phase 0 🔴/🟡 → **不追加任何算力 Capex（第 2 张卡/多机），本地单卡足够验证**。
- **单点故障红线**：本地工作站是唯一算力节点，知识库 + LoRA + 案例库**必须异地备份**（见 `32` §9），否则整厂停摆无退路。
- M3.5 未过 → **不投入全动画产能**，按 `52` 触发 de-scope。
- 单集成本未核算清楚前 → **不承诺更新频率/排播表**。

---

# 6. ROI 探针与变现路径对照

| 变现路径 | 对应形式 | 探测时机 |
|---------|---------|---------|
| 平台创作激励/分成 | A/B 均可 | Phase 0 即探 |
| 付费收听 / 付费章节 | 有声书 / 网文 | Phase 0–1 |
| 短剧分账 | 动态漫/短视频 | Phase 1–2 |
| IP / 版权授权 | 跑出热度后 | Phase 2+ |

> 变现信号采集口径并入 `72-评测工程EvalHarness.md` 的市场评测套件。

---

# 7. 单集成本核算方法论（Unit Economics）

> "单集成本未核算清楚前不承诺更新频率"（§5）—— 本节定义如何核算。

## 7.1 成本结构（按形式）

### 形式 A：有声书/广播剧（章节）

```
单章成本 = LLM写作 + LLM审校 + TTS配音 + 人工工时

LLM写作   = 输入token数 × 输入单价 + 输出token数 × 输出单价
LLM审校   = 同上（审校 profile 通常更短）
TTS       = 字符数 × TTS单价（本地为0，云API按量）
人工工时   = (剧情修改 + 美工校对 + 代码维护) × 月薪 ÷ 产出章节数
```

### 形式 B：动态漫/短视频（分钟）

```
单分钟成本 = 小说成本（按字数折算）+ 图像生成 + LoRA训练摊销 + 视频生成 + 配音 + 人工

图像生成   = 图片数量 × GPU时长 × 云GPU单价
LoRA训练   = 训练时长 × 云GPU单价 ÷ 复用次数（摊销）
视频生成   = 视频秒数 × GPU时长系数 × 云GPU单价
```

## 7.2 成本核算表（每 Phase 填入真实数据）

| 形式 | 单位 | 变量成本（待填） | 人工成本摊销（待填） | 单位总成本（待填） |
|------|------|----------------|-------------------|----------------|
| 有声书 | /章（3000字） | ____元 | ____元 | ____元 |
| 动态漫 | /分钟 | ____元 | ____元 | ____元 |
| 全动画 | /分钟 | ____元（预估） | ____元 | ____元 |

> Phase 1 第一个月即开始记录真实数据，每月更新此表。

## 7.3 可持续产出判断线

| 指标 | 判断方式 |
|------|---------|
| 变现 > 成本 | 当平台分成/创作激励 ≥ 月 Opex，即盈亏平衡 |
| 规模化临界点 | 单集成本随产量下降速度（学习曲线），判断何时值得投 Capex |
| ROI 正转 | 变现收入 / (Capex 摊销 + Opex) ≥ 1，启动规模化 |

> 成本监控脚本并入 `72-评测工程EvalHarness.md` §2.4（经济性评测套件）。
